<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2-debug</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/562d24879a.js" crossorigin="anonymous"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <%= render 'layouts/header' %>
  <main>
    <div class='container px-5 px-sm-0'>
      <div class='row'>
        <div class='col-md-3'>
          <p id="notice"><%= notice %></p>
        </div>
        <div class='col-md-8 offset-md-1'>
          <% if user_signed_in? %>
            <%= form_tag search_path, method: :get do %>
              <%= text_field_tag 'content' %>
              <%= select_tag 'model', options_for_select({"User" => "user", "Book" => "book"}) %>
              <%= select_tag 'method', options_for_select({"完全一致" => "perfect", "前方一致" => "front", "後方一致" => "back", "部分一致" => "partial"}) %>
              <%= submit_tag '検索' %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
    <%= yield %>
  </main>
    <%= render 'layouts/footer' %>
  </body>
</html>